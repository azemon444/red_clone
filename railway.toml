[build]
builder = "DOCKERFILE"
dockerfilePath = "mock-server/Dockerfile"
watchPatterns = ["mock-server/**", "patched-app/assets/default/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
